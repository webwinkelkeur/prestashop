#!/bin/bash

set -euo pipefail

cd "$(dirname "$0")/.."

git checkout master
git push origin master

version="$(git describe --tags --match 'v*')"

echo "Packaging version $version..." >&2
mkdir -p dist
git archive -o "dist/prestashop-webwinkelkeur-$version.zip" master -- webwinkelkeur
