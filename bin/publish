#!/bin/bash

set -euo pipefail
cd "$(dirname "$0")/.."

git checkout master
git push --tags origin master
git push --tags git@github.com:apeschar/prestashop-webwinkelkeur.git master

./bin/package

rsync -av dist/prestashop-webwinkelkeur.zip wwk16@atlas:public_html/download/modules/
